
一、后端代码开发规范
本项目所有后端代码采用go-zero框架编写，所以应遵循go-zero开发流程
1、编写接口第一步编写后缀为.api的接口文件，需定义分组开发，例如：
@server (
	group:      "app"
	prefix:     "/api/admin"
	jwt:        Auth
	middleware: AdminAuthMiddleware
)
service power-api {
    <!-- 接口列表 -->
}
2、在根据编写的api文件执行goctl命令，通常会在后端根目录下定义好Makefile文件，执行该Makefile文件命令中的goctl命令即可生成对应的router、handler以及logic文件
例如：管理后台的make gen, 执行的实际就是： goctl api  go  --api .\api\admin.api --home .\common\template\   --dir .  --style gozero ；

3、注意：并不是经过goctl命令生成完后接口就自动开发完了，此时生成的logic文件都是接口模板文件，只需要删除其中的todo说明，并在其下方完成接口的开发即可

4、开发的配置文件是后缀为yaml的文件，所有数据库、Redis相关的文件均在其中配置， 本地开发mysql账号和密码都是root

5、配置以及所有初始化的操作都需要在svc/servicecontext.go中完成，定义的中间件也需要在此文件中完成初始化

6、所有插件系统在项目根目录下的plugins中完成开发，插件的后台管理菜单仍集成于本管理后台power-admin；plugins目录下相当于又是一个一个全新的项目的目录，然后其目录中有包含了自己的前后端

7、数据库命名请遵循标识符前缀规则，比如power-admin的所有表都命名为admin_XXXX,  cms相关的表都是cms_xxxxx

8、所有数据库插入操作请写go方法来插入，不要写powershell和cmd命令来操作

9、插入菜单数据时也要插入前端组件名称，定义好组件名称再去开发前端页面


二、前端开发规范

1、所有提示、错误、警告等信息不要用alert